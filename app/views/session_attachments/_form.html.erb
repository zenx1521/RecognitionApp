<%= form_for(session_attachment) do |f| %>
  <% if session_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session_attachment.errors.count, "error") %> prohibited this session_attachment from being saved:</h2>

      <ul>
      <% session_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session_id %>
    <%= f.number_field :session_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
